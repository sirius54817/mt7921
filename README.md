# mt7921


Important files:
- `mt7921-no-autoreset.sh` — no-autoreset fix (important)
- `safe-mt7921-fix.sh` — safe fix (important)

Notes:
- The `mt7921-no-autoreset.sh` and `safe-mt7921-fix.sh` scripts are the most
	this two file file all the error made by other file 
    this only for arch linux on gnome and for the bluethooth audio problems to fix only run this script mt7921-no-autoreset.sh` 
